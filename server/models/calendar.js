const models = require('../models');

// interacts with the DB to get information
// return an array of date objects
// [
//   {
//     "date": "2020-10-30",
//     "available": false,
//   }
// ]

module.exports = {
  getDates: function(callback) {
    // query the database to grab the dates and availability status
  }

};
